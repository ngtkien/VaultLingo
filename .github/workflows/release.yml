name: Release Build & Packaging

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Test & Build Release Binaries
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            pkg_name: vaultlingo-linux-amd64
          - os: macos-latest
            goos: darwin
            goarch: arm64
            pkg_name: vaultlingo-macos-arm64

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache: true

      - name: Install Linux Dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev pkg-config libwebkit2gtk-4.1-dev || sudo apt-get install -y libgtk-3-dev pkg-config libwebkit2gtk-4.0-dev

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Run Backend Go Unit Tests
        run: go test -v ./backend/...

      - name: Run Frontend Unit Tests & Build
        run: |
          cd frontend
          npm ci
          npm run test
          npm run check
          npm run build
          cd ..

      - name: Build Desktop GUI & CLI Binaries
        run: |
          wails build -platform ${{ matrix.goos }}/${{ matrix.goarch }}
          go build -ldflags="-s -w" -o build/bin/vl ./cmd/vl

      - name: Package Distribution Bundle (.tar.gz & .deb)
        run: |
          mkdir -p dist/${{ matrix.pkg_name }}
          cp -r build/bin/* dist/${{ matrix.pkg_name }}/
          cp vaultlingo.desktop dist/${{ matrix.pkg_name }}/ 2>/dev/null || true
          cp frontend/src/assets/images/pegasus-logo.png dist/${{ matrix.pkg_name }}/vaultlingo.png 2>/dev/null || true
          cp scripts/install.sh dist/${{ matrix.pkg_name }}/install.sh 2>/dev/null || true
          chmod +x dist/${{ matrix.pkg_name }}/install.sh 2>/dev/null || true
          
          cd dist
          tar -czvf ${{ matrix.pkg_name }}.tar.gz ${{ matrix.pkg_name }}
          cd ..

          if [ "${{ runner.os }}" = "Linux" ]; then
            chmod +x scripts/package_deb.sh
            ./scripts/package_deb.sh
          fi

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.tar.gz
            dist/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
